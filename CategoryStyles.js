import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  categoryStyles: {
    backgroundColor: '#000',
  },

  flatListStyles: {
    justifyContent: 'center',
    alignItems: 'center',
    padding: 8,
    paddingRight: 0
  },

  itemStyles: {
    padding: 8,
    marginRight: 8,
    borderRadius: 5,
    justifyContent: 'center',
    alignItems: 'center'
  },

  textItemStyles: {
    textAlign: 'center'
  }
});
